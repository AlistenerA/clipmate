# MANUAL_QA_RESULT_TEMPLATE.md — ClipMate v0.2 手动测试记录

> 逐项手动测试后填写。通过标记 [x]，失败标记 [ ] 并记录现象。

---

## 测试信息

- **测试日期**：____年__月__日
- **测试人员**：________
- **Edge 版本**：________
- **构建产物**：`clipmate-v0.2/dist/`
- **Notion Token**：已配置 / 未配置
- **测试页面 1**：________
- **测试页面 2**：________

---

## 一、安装与加载

- [ ] **TC-01 安装测试**：Edge 加载 `dist/` 成功，无错误，名称「ClipMate」，版本 `0.2.0`
  > 备注：

- [ ] **TC-02 加载验证**：工具栏图标正常，Popup 可打开，Options 可打开
  > 备注：

---

## 二、Options 设置页

- [ ] **TC-03 Notion Token 保存**：填写 Token → 保存 → 关闭重开 → Token 不丢失
  > 备注：

- [ ] **TC-04 添加 Notion 目标**：点击添加 → 填名称 + Page ID → 保存 → 出现在列表
  > 备注：

- [ ] **TC-05 编辑 Notion 目标**：编辑 → 修改名称/ID → 保存 → 更新正确
  > 备注：

- [ ] **TC-06 删除 Notion 目标**：删除非默认 → 成功；删默认 → 自动选新默认
  > 备注：

- [ ] **TC-07 设置默认目标**：设默认 → 标记转移 → 仅一个 isDefault=true
  > 备注：

---

## 三、Popup 剪藏

- [ ] **TC-08 Popup 默认目标选中**：Popup 目标下拉框默认选中默认目标
  > 备注：

- [ ] **TC-09 Popup 切换目标保存**：切换目标 → 保存 → 内容到正确 Notion 页面
  > 备注：

- [ ] **TC-10 全文剪藏保存**：全文 → 加标签/备注 → 保存成功 → Notion 内容完整
  - 保存耗时：约____秒
  > 备注：

- [ ] **TC-11 选区剪藏保存**：选中文字 → 选区模式 → 保存成功
  - 选中字数：________
  > 备注：

- [ ] **TC-12 标签保存**：添加标签 → 保存 → Notion 中蓝色标签文字正确
  > 备注：

- [ ] **TC-13 备注保存**：填写备注 → 保存 → Notion 中 📝 callout 正确
  > 备注：

- [ ] **TC-14 复制 Markdown**：复制 → 粘贴 → 标题/URL/标签/备注/分割线/正文六部分完整
  - 无 `****` 异常粗体：是 / 否
  > 备注：

---

## 四、本地剪藏历史

- [ ] **TC-15 保存成功写入历史**：成功保存 → History 中出现「已保存」记录
  > 备注：

- [ ] **TC-16 保存失败写入 failed 历史**：失败 → History 中出现「失败」记录 + 错误原因
  > 备注：

- [ ] **TC-17 历史搜索标题**：搜索标题关键词 → 过滤正确，匹配文字高亮
  > 备注：

- [ ] **TC-18 历史搜索 URL / 域名**：搜索域名 → 过滤正确
  > 备注：

- [ ] **TC-19 历史搜索标签**：搜索标签关键词 → 过滤正确，标签命中黄底高亮
  > 备注：

- [ ] **TC-20 历史搜索正文 — 正文匹配**：搜索正文词 → 显示橙色「正文匹配」tag
  > 备注：

- [ ] **TC-21 历史搜索备注 — 备注匹配**：搜索备注词 → 显示青色「备注匹配」tag
  > 备注：

- [ ] **TC-22 历史复制 Markdown**：历史条目复制 → 粘贴完整
  > 备注：

- [ ] **TC-23 历史删除单条**：删除按钮 → 条目移除
  > 备注：

- [ ] **TC-24 历史清空全部**：清空 → 确认 → 全部移除
  > 备注：

- [ ] **TC-25 failed/unsaved 历史重试保存**：重试按钮 → 选择目标 → 状态更新，不重复
  > 备注：

---

## 五、History UI 体验

- [ ] **TC-26 favicon 真实图标优先**：有 favicon 站点 → 条目显示真实图标
  > 备注：

- [ ] **TC-27 favicon fallback 首字母**：无 favicon 站点 → 显示域名首字母头像
  > 备注：

- [ ] **TC-28 图片开篇摘要清理**：开篇图片文章 → 摘要不显示 `![]()` 语法
  - 测试页面：________
  > 备注：

- [ ] **TC-29 同站色条一致**：同站多条历史 → 左侧色条颜色相同
  > 备注：

- [ ] **TC-30 同站不同文章 Popup 自动刷新**：切换文章 → 开 Popup → 新内容正确
  > 备注：

- [ ] **TC-31 同页重复打开不丢失标签/备注**：填标签/备注 → 关 Popup → 同页重开 → 恢复
  > 备注：

---

## 六、设置与存储

- [ ] **TC-32 关闭历史不写新记录**：关闭开关 → 保存 → 无新历史
  > 备注：

- [ ] **TC-33 historyLimit 裁剪**：设 limit=10 → 存 11 条 → 最早一条被裁
  > 备注：

---

## 七、错误提示

- [ ] **TC-34 Token 缺失**：无 Token → 保存 → 提示"请先配置 Notion Token"
  > 备注：

- [ ] **TC-35 目标缺失**：无目标 → 保存 → 提示"请先添加 Notion 目标页面"
  > 备注：

- [ ] **TC-36 Notion API 失败**：无效 Token → 友好提示，Token 不泄露
  > 备注：

---

## 八、隐私与打包

- [ ] **TC-37 隐私检查**：Console 无 Token / 正文全文 / 备注全文 / 完整 Markdown
  - Token 泄露：否
  - 正文泄露：否
  - 备注泄露：否
  - Markdown 泄露：否
  > 备注：

- [ ] **TC-38 Build 测试**：`npm run build` 成功，dist/manifest.json version=0.2.0
  - Build 耗时：____ms
  > 备注：

- [ ] **TC-39 Zip 测试**：`npm run zip` 成功，clipmate-v0.2.zip 生成
  - Zip 大小：________
  > 备注：

- [ ] **TC-40 Zip 内容检查**：zip 仅含 dist，无源码/测试/文档/node_modules/.env
  - 含 manifest.json：是 / 否
  - 含 icons：是 / 否
  - 不含 .ts/.tsx 源文件：是 / 否
  > 备注：

---

## 九、总体评估

- 通过 / 未通过的用例：____ / 40
- 阻塞问题：有 / 无
- 是否可以进入人工验收 B：是 / 否
- 是否可以提交 Edge Add-ons 审核：是 / 否

> 签名：________  日期：____年__月__日
