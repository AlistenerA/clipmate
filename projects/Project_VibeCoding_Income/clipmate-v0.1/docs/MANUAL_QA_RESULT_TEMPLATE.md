# MANUAL_QA_RESULT_TEMPLATE.md — ClipMate v0.1 手动测试记录

> 逐项手动测试后填写。通过标记 [x]，失败标记 [ ] 并记录现象。

---

## 测试信息

- **测试日期**：____年__月__日
- **测试人员**：________
- **Edge 版本**：________
- **构建产物**：`clipmate-v0.1/dist/`
- **Notion Token**：已配置 / 未配置
- **测试页面 1**：________
- **测试页面 2**：________

---

## 一、安装与加载

- [ ] **TC-01 安装测试**：Edge 加载 `dist/` 成功，无错误，显示「ClipMate」名称，版本 `0.1.0`
  > 备注：

- [ ] **TC-02 加载验证**：工具栏图标正常显示，Popup 可打开，Options 页可打开
  > 备注：

---

## 二、UI 与设置

- [ ] **TC-03 Popup 打开**：模式切换、状态栏、内容预览、标签、备注、按钮、设置图标全部正常渲染
  > 备注：

- [ ] **TC-04 Options 保存配置**：填写 Token/Page ID/标签 → 保存 → 关闭重开 → 配置不丢失
  > 备注：

---

## 三、正文提取

- [ ] **TC-05 Fullpage 正文提取**：博客文章正文提取正确，无导航栏/广告混入，元数据正确
  - 测试页面：________
  - 字数统计：________
  > 备注：

- [ ] **TC-06 Selection 选区剪藏**：选中文字提取正确，粗体/链接格式保留，字数统计准确
  - 选中字数：________
  > 备注：

---

## 四、复制 Markdown

- [ ] **TC-07 复制 Markdown**：粘贴结果包含标题、URL、标签、备注、分割线、正文六部分
  - 无 `****` 异常粗体：是 / 否
  - 长备注 callout 保持：是 / 否
  > 备注：

---

## 五、Notion 保存

- [ ] **TC-08 保存到 Notion（正常）**：全文模式下提取 → 加标签/备注 → 保存成功 → Notion 页面内容完整
  - 保存耗时：约____秒
  - Notion 内容正确：是 / 否
  > 备注：

- [ ] **TC-09 无选区错误**：选区模式无选中 → 显示「请先选中页面文字后再提取」
  > 备注：

- [ ] **TC-10 Token 缺失**：清空 Token → 保存到 Notion → 提示「请先在设置中配置 Notion Token」
  > 备注：

- [ ] **TC-11 Token 错误**：无效 Token → 保存到 Notion → 提示授权失败，Token 不泄露
  > 备注：

- [ ] **TC-12 Page ID 错误**：错误 Page ID → 保存到 Notion → 友好错误提示
  > 备注：

- [ ] **TC-13 Integration 未授权**：未添加到页面 Connections → 保存失败 → 友好提示
  > 备注：

---

## 六、边界场景

- [ ] **TC-14 Content Script 限制**：`edge://` 页面 → Popup 可打开 → 不可提取 → 有提示
  > 备注：

---

## 七、隐私与安全

- [ ] **TC-15 隐私与日志**：DevTools Console 中无 Token、无正文全文、无备注全文泄露
  - Token 泄露：否
  - 正文泄露：否
  - 备注泄露：否
  > 备注：

---

## 八、打包

- [ ] **TC-16 Build 打包**：`npm run build` 成功 → zip 产物不包含源码/测试/文档
  - Build 耗时：____ms
  - Zip 文件名：________
  - Zip 大小：________
  > 备注：

---

## 九、总体评估

- 通过 / 未通过的用例：____ / 16
- 阻塞问题：有 / 无
- 是否可以进入人工验收 B：是 / 否
- 是否可以提交 Edge Add-ons 审核：是 / 否

> 签名：________  日期：____年__月__日
