# AGENTS.md

This repo is currently an empty scaffold with two placeholder directories (`porj01/`, `Test/`). No build system, language, framework, or toolchain is configured yet.

- No lint, test, or typecheck commands exist.
- No CI, no pre-commit hooks, no package manifests.
- `.gitattributes` only sets `* text=auto`.
